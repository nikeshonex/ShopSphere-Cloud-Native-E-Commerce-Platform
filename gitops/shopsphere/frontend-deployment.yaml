apiVersion: apps/v1
kind: Deployment

metadata:
  name: shopsphere-frontend
  namespace: shopsphere

spec:
  replicas: 2

  selector:
    matchLabels:
      app: shopsphere-frontend

  template:
    metadata:
      labels:
        app: shopsphere-frontend

    spec:
      containers:
        - name: frontend
          image: /shopsphere-frontend:

          ports:
            - containerPort: 80

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"

