apiVersion: v1
kind: Service

metadata:
  name: shopsphere-frontend
  namespace: shopsphere

spec:
  type: ClusterIP

  selector:
    app: shopsphere-frontend

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

